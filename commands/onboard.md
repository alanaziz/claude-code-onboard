---
description: "Set up your AI workspace with a 10-minute guided onboarding"
---

Use the `claude-code-onboard:onboard` skill to run the full onboarding flow.

If `~/Desktop/OS/` already exists with a `CLAUDE.md` and `context/` folder, warn the user that running `/onboard` again will overwrite their existing context files. Suggest `/update-context` if they only want to refresh specific sections.
